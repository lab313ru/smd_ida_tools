#include "drawutil.h"

// the only purpose this currently serves is to be a file I can compile
// to make sure drawutil.h doesn't have noncompiling stuff in it
// but if any functions too large to be inlined go into DrawUtil they should be implemented here